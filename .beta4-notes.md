## CRITICAL BUG FIX

### 🔴 Major Fix
- Fixed config flow handler NOT being registered
- Added import of CutremureConfigFlow in __init__.py
- Home Assistant now properly discovers the config flow
- Completely fixes 'Invalid handler specified' error

### The Problem
- config_flow.py was defined but never imported
- Home Assistant couldn't find the handler during discovery
- Users got 'Invalid handler specified' error

### The Solution
- CutremureConfigFlow now properly imported in __init__.py
- DOMAIN constant correctly referenced
- Config flow is discoverable and loadable

### Installation
Via HACS:
- Custom repositories → Add: https://github.com/dancucu/cutremure-infp-romania
- Category: Integration → Install

Or Manual:
- Copy custom_components/cutremure_infp to ~/.homeassistant/custom_components/
- Restart Home Assistant
- Settings → Devices & Services → Create Integration → Cutremure INFP

This version should work without errors. Config flow will now load properly.
